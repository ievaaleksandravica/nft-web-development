pragma solidity >=0.7.0 <0.9.0;

contract Types {
    // to check if it is true or false
    bool public lieDetector;

    // integer to hold the amount
    uint256 public walletAmount;

    // store text information about the error
    string public errorMessage = "there has been a mistake!";
}
